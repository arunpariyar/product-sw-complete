{
  "name": "nuxt-app",
  "type": "module",
  "private": true,
  "prisma": {
    "seed": "npx ts-node prisma/seed.ts"
  },
  "scripts": {
    "predev": "npx prisma generate && npx ts-node prisma/seed.ts",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@prisma/client": "^6.16.3",
    "nuxt": "^4.1.2",
    "prisma": "^6.16.3",
    "vue": "^3.5.22",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.2"
  }
}
